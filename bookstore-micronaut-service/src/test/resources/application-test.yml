micronaut:
  port: 8083

datasources:
  default:
    url: jdbc:h2:mem:devDb;
    driverClassName: org.h2.Driver
    username: test
    password: test
    schema-generate: CREATE_DROP

jpa:
  default:
    properties:
      hibernate:
        show_sql: false

logger:
  levels:
    liquibase: OFF