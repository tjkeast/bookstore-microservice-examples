micronaut:
  port: 8083

datasources:
  default:
    url: jdbc:h2:mem:devDb;
    driverClassName: org.h2.Driver
    username: test
    password: test

jpa:
  default:
    properties:
      hibernate:
        show_sql: true

logger:
  levels:
    liquibase: OFF